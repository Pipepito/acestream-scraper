---
phase: 01-parity-baseline-and-safety-gates
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - v2/backend/tests/parity/baseline_sources.yaml
  - v2/backend/tests/parity/parity_manifest.py
  - v2/backend/tests/parity/parity_compare.py
  - v2/backend/tests/parity/test_scraper_parity.py
  - v2/backend/tests/parity/test_output_parity.py
  - v2/backend/tests/parity/snapshots/README.md
autonomous: true
requirements:
  - SCRP-01
  - SCRP-02
  - SCRP-03
  - SCRP-04
must_haves:
  truths:
    - "HTTP/M3U and ZeroNet scrape outputs are compared against a governed baseline dataset."
    - "Core channel fields and source linkage regressions are detected automatically."
    - "Playlist and EPG artifacts are validated against parity expectations."
  artifacts:
    - path: v2/backend/tests/parity/baseline_sources.yaml
      provides: "Change-controlled source inventory including source class and environment tags."
    - path: v2/backend/tests/parity/parity_compare.py
      provides: "Field-level and fuzzy metadata parity comparison logic."
    - path: v2/backend/tests/parity/test_scraper_parity.py
      provides: "Automated regression coverage for HTTP/M3U and ZeroNet scraper behavior."
    - path: v2/backend/tests/parity/test_output_parity.py
      provides: "Automated playlist and EPG parity/validity checks."
  key_links:
    - from: v2/backend/tests/parity/baseline_sources.yaml
      to: v2/backend/tests/parity/test_scraper_parity.py
      via: "manifest loading in test setup"
      pattern: "load.*baseline"
    - from: v2/backend/tests/parity/parity_compare.py
      to: v2/backend/tests/parity/test_scraper_parity.py
      via: "comparator assertions"
      pattern: "compare_.*parity"
    - from: v2/backend/tests/parity/test_output_parity.py
      to: v2/backend/app/services/playlist_service.py
      via: "playlist/EPG generation invocation"
      pattern: "playlist|epg"
---

<objective>
Define and implement the parity baseline harness used to prove scraper behavior is preserved during migration.

Purpose: Establish objective, repeatable evidence for SCRP-01 through SCRP-04 before deeper refactors.
Output: Baseline manifest, comparator utilities, and parity tests for scraping + output artifacts.
</objective>

<execution_context>
@/Users/pipepito/.codex/get-shit-done/workflows/execute-plan.md
@/Users/pipepito/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-parity-baseline-and-safety-gates/01-CONTEXT.md
@.planning/phases/01-parity-baseline-and-safety-gates/01-RESEARCH.md

@v2/backend/app/services/scraper_service.py
@v2/backend/app/scrapers/http.py
@v2/backend/app/scrapers/zeronet.py
@v2/backend/app/services/playlist_service.py
@v2/backend/app/services/epg_service.py
@v2/backend/tests/test_scrapers.py
@v2/backend/tests/test_playlists.py
@v2/backend/tests/test_epg.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create governed baseline source manifest and snapshot contract</name>
  <files>v2/backend/tests/parity/baseline_sources.yaml, v2/backend/tests/parity/snapshots/README.md</files>
  <action>Define the full baseline inventory from configured sources, including active/flaky/disabled/legacy class, URL type (HTTP/M3U/ZeroNet), and environment requirement tags for auth/region dependencies. Document snapshot versioning and explicit approval rules so baseline changes require review rather than silent drift.</action>
  <verify>pytest -q v2/backend/tests/parity/test_scraper_parity.py::test_baseline_manifest_schema</verify>
  <done>Baseline manifest encodes all source classes and snapshot governance; schema validation test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement parity comparator utilities for strict and fuzzy field checks</name>
  <files>v2/backend/tests/parity/parity_manifest.py, v2/backend/tests/parity/parity_compare.py</files>
  <action>Implement reusable comparator helpers that enforce strict parity for identity/linkage fields (`id`, `tvg_id`, source linkage), strict presence checks for required fields (`id`, `name`, `group`, `logo`, `tvg_id`, `tvg_name`), and fuzzy tolerance for noisy metadata fields per context guidance. Include separate scoring paths for gate-critical vs non-blocking source classes.</action>
  <verify>pytest -q v2/backend/tests/parity/test_scraper_parity.py::test_parity_comparator_rules</verify>
  <done>Comparator module returns deterministic parity verdicts and exposes category-aware scoring used by parity tests.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated parity regression tests for scraping, playlist, and EPG outputs</name>
  <files>v2/backend/tests/parity/test_scraper_parity.py, v2/backend/tests/parity/test_output_parity.py</files>
  <action>Build regression tests that execute representative scrape flows for HTTP/M3U and ZeroNet inputs against baseline snapshots, assert field-level parity requirements, and validate playlist/EPG output validity. Tests must produce actionable failure messages that identify source class, source id, and mismatch type.</action>
  <verify>pytest -q v2/backend/tests/parity/test_scraper_parity.py v2/backend/tests/parity/test_output_parity.py</verify>
  <done>Parity suite catches regressions in scraper behavior and output artifacts with source-class-aware reporting.</done>
</task>

</tasks>

<verification>
- [ ] `pytest -q v2/backend/tests/parity/test_scraper_parity.py`
- [ ] `pytest -q v2/backend/tests/parity/test_output_parity.py`
- [ ] Existing smoke tests still pass: `pytest -q v2/backend/tests/test_scrapers.py v2/backend/tests/test_playlists.py v2/backend/tests/test_epg.py`
</verification>

<success_criteria>
- All tasks completed
- SCRP-01, SCRP-02, SCRP-03, and SCRP-04 are covered by executable parity tests
- Baseline manifest and snapshots are versioned and explicitly governable
- Gate-critical and non-blocking source classes are separated in parity output
</success_criteria>

<output>
After completion, create `.planning/phases/01-parity-baseline-and-safety-gates/01-01-SUMMARY.md`
</output>
