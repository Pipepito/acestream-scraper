---
phase: 01-parity-baseline-and-safety-gates
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - scripts/phase_gates/phase1_gate_config.yaml
  - scripts/phase_gates/phase1_gate_runner.py
  - .github/workflows/phase1-safety-gates.yml
  - docs/migration/phase1-parity-gates.md
autonomous: true
requirements:
  - SCRP-04
  - QUAL-04
must_haves:
  truths:
    - "Phase transitions run objective parity gates before approval."
    - "Gate output clearly separates blocking failures from non-blocking legacy/disabled source drift."
    - "Cutover readiness can be proven with an auditable checklist and command output."
  artifacts:
    - path: scripts/phase_gates/phase1_gate_runner.py
      provides: "Single command entry point for Phase 1 parity gate execution and reporting."
    - path: scripts/phase_gates/phase1_gate_config.yaml
      provides: "Configurable gate thresholds, source class policy, and command profiles."
    - path: .github/workflows/phase1-safety-gates.yml
      provides: "Automated CI enforcement of parity gate execution."
    - path: docs/migration/phase1-parity-gates.md
      provides: "Operator-facing checklist and evidence collection procedure."
  key_links:
    - from: scripts/phase_gates/phase1_gate_runner.py
      to: backend/tests/parity/test_scraper_parity.py
      via: "subprocess pytest invocation profile"
      pattern: "pytest.*test_scraper_parity"
    - from: scripts/phase_gates/phase1_gate_runner.py
      to: scripts/phase_gates/phase1_gate_config.yaml
      via: "runtime config load for threshold policy"
      pattern: "phase1_gate_config"
    - from: .github/workflows/phase1-safety-gates.yml
      to: scripts/phase_gates/phase1_gate_runner.py
      via: "workflow job command"
      pattern: "phase1_gate_runner.py"
---

<objective>
Operationalize Phase 1 parity checks as reusable migration safety gates and a documented acceptance checklist.

Purpose: Ensure QUAL-04 is satisfied with measurable, repeatable acceptance criteria that block unsafe cutover transitions.
Output: Gate runner + config, CI hook, and migration checklist documentation.
</objective>

<execution_context>
@/Users/pipepito/.codex/get-shit-done/workflows/execute-plan.md
@/Users/pipepito/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-parity-baseline-and-safety-gates/01-CONTEXT.md
@.planning/phases/01-parity-baseline-and-safety-gates/01-RESEARCH.md
@.planning/phases/01-parity-baseline-and-safety-gates/01-01-PLAN.md

@.github/workflows/pull_request.yml
@backend/tests/test_scrapers.py
@backend/tests/test_playlists.py
@backend/tests/test_epg.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build phase safety gate runner with class-aware pass/fail rules</name>
  <files>scripts/phase_gates/phase1_gate_config.yaml, scripts/phase_gates/phase1_gate_runner.py</files>
  <action>Create a configurable gate runner that executes Phase 1 parity test commands, parses outcomes into gate-critical vs non-blocking source classes, and exits non-zero only when blocking criteria fail. Include at least `quick` and `full` profiles to support PR checks and pre-cutover checks.</action>
  <verify>python scripts/phase_gates/phase1_gate_runner.py --profile quick --dry-run</verify>
  <done>Runner prints profile-specific execution plan and scoring policy; blocking/non-blocking categories are explicit in output format.</done>
</task>

<task type="auto">
  <name>Task 2: Wire parity safety gates into CI execution path</name>
  <files>.github/workflows/phase1-safety-gates.yml</files>
  <action>Add a dedicated workflow that installs backend dependencies, runs the phase gate runner in `quick` profile on pull requests, and preserves machine-readable gate logs as artifacts. Keep workflow independent enough to be reused by later phases as the canonical gate entrypoint.</action>
  <verify>rg -n \"phase1_gate_runner.py|--profile quick\" .github/workflows/phase1-safety-gates.yml</verify>
  <done>CI workflow can invoke the gate runner and fail reliably when gate-critical parity regressions are present.</done>
</task>

<task type="auto">
  <name>Task 3: Publish migration acceptance checklist and evidence requirements</name>
  <files>docs/migration/phase1-parity-gates.md</files>
  <action>Document the operator checklist for phase acceptance: required commands, required artifacts, pass/fail interpretation, snapshot change-control expectations, and sign-off evidence. Explicitly capture how auth/region-dependent and legacy/disabled sources are reported but handled per policy.</action>
  <verify>rg -n \"blocking|non-blocking|snapshot|evidence|sign-off\" docs/migration/phase1-parity-gates.md</verify>
  <done>Checklist is executable by a reviewer with no tribal knowledge and produces auditable acceptance evidence.</done>
</task>

</tasks>

<verification>
- [ ] `python scripts/phase_gates/phase1_gate_runner.py --profile quick --dry-run`
- [ ] `python scripts/phase_gates/phase1_gate_runner.py --profile full --dry-run`
- [ ] `pytest -q backend/tests/parity/test_scraper_parity.py backend/tests/parity/test_output_parity.py`
</verification>

<success_criteria>
- All tasks completed
- QUAL-04 acceptance gate is automated, documented, and reusable
- SCRP-04 output validation is part of gate execution profiles
- CI and manual pre-cutover checks share the same gate runner entrypoint
</success_criteria>

<output>
After completion, create `.planning/phases/01-parity-baseline-and-safety-gates/01-02-SUMMARY.md`
</output>
