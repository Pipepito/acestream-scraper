---
phase: 02-backend-contract-and-structure-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - v2/backend/app/schemas/channel.py
  - v2/backend/app/schemas/epg.py
  - v2/backend/app/schemas/config.py
  - v2/backend/app/api/endpoints/channels.py
  - v2/backend/app/api/endpoints/tv_channels.py
  - v2/backend/app/api/endpoints/epg.py
  - v2/backend/app/api/endpoints/config.py
  - v2/frontend/src/services/channelService.ts
  - v2/frontend/src/services/configService.ts
  - v2/backend/tests/contracts/test_channel_contracts.py
  - v2/backend/tests/contracts/test_config_contracts.py
autonomous: true
requirements:
  - API-01
  - MIGR-03
must_haves:
  truths:
    - "Core v2 endpoints use explicit request/response schemas with no ambiguous runtime payload parsing."
    - "Channels/TV/config/EPG contracts are consistent across backend and frontend service consumers."
    - "Contract regression tests fail on payload shape drift before runtime regressions reach users."
  artifacts:
    - path: v2/backend/app/schemas/channel.py
      provides: "Typed DTOs for bulk channel and TV-channel operations with stable envelope shapes."
    - path: v2/backend/app/schemas/config.py
      provides: "Typed config update and response envelopes replacing mixed raw JSON parsing."
    - path: v2/backend/app/api/endpoints/tv_channels.py
      provides: "Typed request/response boundary for TV-channel list and association workflows."
    - path: v2/frontend/src/services/channelService.ts
      provides: "Frontend service typings aligned with paginated backend channel response contract."
    - path: v2/backend/tests/contracts/test_channel_contracts.py
      provides: "Automated enforcement of channel and TV-channel contract invariants."
    - path: v2/backend/tests/contracts/test_config_contracts.py
      provides: "Automated enforcement of config endpoint contract invariants."
  key_links:
    - from: v2/backend/app/schemas/channel.py
      to: v2/backend/app/api/endpoints/channels.py
      via: "request and response model annotations"
      pattern: "response_model|Body"
    - from: v2/backend/app/schemas/config.py
      to: v2/backend/app/api/endpoints/config.py
      via: "typed update DTO usage"
      pattern: "BaseModel|response_model"
    - from: v2/backend/app/api/endpoints/channels.py
      to: v2/frontend/src/services/channelService.ts
      via: "paginated envelope contract {items,total}"
      pattern: "items|total"
    - from: v2/backend/tests/contracts/test_channel_contracts.py
      to: v2/backend/app/api/endpoints/tv_channels.py
      via: "endpoint response/body contract assertions"
      pattern: "/api/v1/tv-channels"
---

<objective>
Normalize v2 API contracts for high-churn endpoints by removing ambiguous payload shapes and enforcing typed request/response schemas.

Purpose: Satisfy API-01 while preserving current user-facing functionality required for MIGR-03.
Output: Typed endpoint contracts, aligned frontend service typings, and contract regression tests.
</objective>

<execution_context>
@/Users/pipepito/.codex/get-shit-done/workflows/execute-plan.md
@/Users/pipepito/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-backend-contract-and-structure-hardening/02-RESEARCH.md

@v2/backend/app/api/endpoints/channels.py
@v2/backend/app/api/endpoints/tv_channels.py
@v2/backend/app/api/endpoints/epg.py
@v2/backend/app/api/endpoints/config.py
@v2/backend/app/schemas/channel.py
@v2/backend/app/schemas/epg.py
@v2/backend/app/schemas/config.py
@v2/frontend/src/services/channelService.ts
@v2/frontend/src/services/configService.ts
@v2/backend/tests/test_channels.py
@v2/backend/tests/test_tv_channels.py
@v2/backend/tests/test_config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define explicit contract DTOs for ambiguous channel/TV/config/EPG operations</name>
  <files>v2/backend/app/schemas/channel.py, v2/backend/app/schemas/epg.py, v2/backend/app/schemas/config.py</files>
  <action>Add dedicated request/response models for currently untyped payloads (bulk channel edit/activate, TV-channel association and batch operations, EPG mapping payloads, and config updates). Keep response envelope conventions stable (`items`/`total` for paginated lists, explicit operation-result schemas for mutations) and avoid `Dict[str, Any]`/raw dict bodies for these paths.</action>
  <verify>pytest -q v2/backend/tests/contracts/test_channel_contracts.py::test_schema_models_cover_bulk_and_association_payloads</verify>
  <done>Typed DTOs exist for all previously ambiguous payloads and expose deterministic field-level validation errors.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor endpoints to consume typed schemas and remove raw JSON shape parsing</name>
  <files>v2/backend/app/api/endpoints/channels.py, v2/backend/app/api/endpoints/tv_channels.py, v2/backend/app/api/endpoints/epg.py, v2/backend/app/api/endpoints/config.py</files>
  <action>Update endpoint signatures to use the new request/response models and replace mixed `request.json()` parsing with validated body models. Remove duplicate config dependency declarations and ensure response models are explicit for all modified endpoints. Preserve route paths and business behavior so existing functional workflows remain intact.</action>
  <verify>pytest -q v2/backend/tests/test_channels.py v2/backend/tests/test_tv_channels.py v2/backend/tests/test_config.py v2/backend/tests/test_epg.py</verify>
  <done>Endpoints no longer rely on ambiguous dict payloads for targeted routes and all existing endpoint integration tests continue to pass.</done>
</task>

<task type="auto">
  <name>Task 3: Align frontend service typings and add backend contract regression tests</name>
  <files>v2/frontend/src/services/channelService.ts, v2/frontend/src/services/configService.ts, v2/backend/tests/contracts/test_channel_contracts.py, v2/backend/tests/contracts/test_config_contracts.py</files>
  <action>Update frontend service return types to match stabilized backend response envelopes and remove `any` usage in targeted methods. Add backend contract tests that assert request validation behavior and response schema shape for channels/TV/config endpoints so future drift is caught early.</action>
  <verify>pytest -q v2/backend/tests/contracts/test_channel_contracts.py v2/backend/tests/contracts/test_config_contracts.py</verify>
  <done>Frontend service types compile against stable contract shapes and contract-focused backend tests protect against shape regressions.</done>
</task>

</tasks>

<verification>
- [ ] `pytest -q v2/backend/tests/contracts/test_channel_contracts.py v2/backend/tests/contracts/test_config_contracts.py`
- [ ] `pytest -q v2/backend/tests/test_channels.py v2/backend/tests/test_tv_channels.py v2/backend/tests/test_config.py v2/backend/tests/test_epg.py`
</verification>

<success_criteria>
- All tasks completed
- API-01 is demonstrably covered with typed contracts and regression checks
- Frontend service type assumptions are aligned with backend envelopes
- MIGR-03 continuity is preserved for channels/TV/config/EPG workflows
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-contract-and-structure-hardening/02-01-SUMMARY.md`
</output>
