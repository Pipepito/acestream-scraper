---
phase: 02-backend-contract-and-structure-hardening
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - backend/app/api/endpoints/urls.py
  - backend/app/api/endpoints/scrapers.py
  - backend/app/api/endpoints/health.py
  - backend/app/services/url_service.py
  - backend/app/services/scraper_service.py
  - backend/app/services/stats_service.py
  - backend/app/repositories/url_repository.py
  - backend/app/repositories/stats_repository.py
  - backend/app/api/dependencies.py
  - backend/tests/architecture/test_layer_boundaries.py
  - backend/tests/test_urls.py
autonomous: true
requirements:
  - API-02
  - MIGR-03
must_haves:
  truths:
    - "Endpoint handlers are HTTP translators and no longer perform direct persistence concerns."
    - "Service modules orchestrate domain behavior while repositories own data access patterns."
    - "Architecture regression checks detect future boundary leakage before merge."
  artifacts:
    - path: backend/app/api/dependencies.py
      provides: "Centralized dependency providers for endpoint-to-service wiring."
    - path: backend/app/repositories/url_repository.py
      provides: "Single owner of URL persistence operations used by URL and scraper workflows."
    - path: backend/app/repositories/stats_repository.py
      provides: "Encapsulated stats query layer replacing service-level direct aggregate queries."
    - path: backend/app/services/url_service.py
      provides: "Repository-backed URL orchestration without direct endpoint coupling."
    - path: backend/tests/architecture/test_layer_boundaries.py
      provides: "Automated boundary guard assertions for endpoint/service/repository responsibilities."
  key_links:
    - from: backend/app/api/endpoints/urls.py
      to: backend/app/services/url_service.py
      via: "dependency injection provider and typed service entrypoint"
      pattern: "Depends|get_.*service"
    - from: backend/app/services/url_service.py
      to: backend/app/repositories/url_repository.py
      via: "service delegates all URL persistence reads/writes to repository"
      pattern: "URLRepository"
    - from: backend/app/services/stats_service.py
      to: backend/app/repositories/stats_repository.py
      via: "aggregate query abstraction for dashboard/health stats"
      pattern: "stats_repository"
    - from: backend/tests/architecture/test_layer_boundaries.py
      to: backend/app/api/endpoints
      via: "static assertions preventing direct db query usage in endpoints"
      pattern: "db.query"
---

<objective>
Refactor backend module boundaries so endpoint, service, and repository responsibilities are explicit and enforceable.

Purpose: Satisfy API-02 and reduce architecture pollution while retaining existing endpoint behavior required for MIGR-03.
Output: Repository-backed service paths, dependency-injected endpoints, and architecture guard tests.
</objective>

<execution_context>
@/Users/pipepito/.codex/get-shit-done/workflows/execute-plan.md
@/Users/pipepito/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-backend-contract-and-structure-hardening/02-RESEARCH.md
@.planning/phases/02-backend-contract-and-structure-hardening/02-01-PLAN.md

@backend/app/api/endpoints/urls.py
@backend/app/api/endpoints/scrapers.py
@backend/app/api/endpoints/health.py
@backend/app/services/url_service.py
@backend/app/services/scraper_service.py
@backend/app/services/stats_service.py
@backend/app/repositories/url_repository.py
@backend/tests/test_scrapers.py
@backend/tests/test_urls.py
@backend/tests/test_health.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce explicit dependency wiring and repository ownership for URL/stats domains</name>
  <files>backend/app/api/dependencies.py, backend/app/repositories/url_repository.py, backend/app/repositories/stats_repository.py</files>
  <action>Create/extend dependency providers so endpoint modules receive fully-wired services rather than hand-built inline dependencies. Expand repository interfaces to own URL refresh/list/update primitives and stats aggregate queries currently scattered across services/endpoints.</action>
  <verify>pytest -q backend/tests/test_urls.py::TestURLRefreshEndpoints::test_refresh_url</verify>
  <done>Dependency wiring is centralized and repositories expose the required URL/stats persistence operations.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor services and endpoints to enforce endpoint -> service -> repository flow</name>
  <files>backend/app/services/url_service.py, backend/app/services/scraper_service.py, backend/app/services/stats_service.py, backend/app/api/endpoints/urls.py, backend/app/api/endpoints/scrapers.py, backend/app/api/endpoints/health.py</files>
  <action>Update targeted services to consume repository abstractions instead of direct ORM calls, and keep endpoint modules limited to request parsing/response translation. Remove direct DB queries from endpoint modules in the targeted paths and preserve current API behavior and route signatures.</action>
  <verify>pytest -q backend/tests/test_urls.py backend/tests/test_scrapers.py backend/tests/test_health.py</verify>
  <done>Targeted endpoint paths no longer contain direct data-access logic, and integration behavior remains compatible with current test expectations.</done>
</task>

<task type="auto">
  <name>Task 3: Add architecture regression checks for layer boundary violations</name>
  <files>backend/tests/architecture/test_layer_boundaries.py</files>
  <action>Add architecture-focused tests that fail when endpoint modules contain disallowed persistence patterns (for example direct `db.query` usage) or when service modules bypass designated repositories in targeted domains. Keep checks narrowly scoped to avoid blocking legitimate low-level modules.</action>
  <verify>pytest -q backend/tests/architecture/test_layer_boundaries.py</verify>
  <done>Automated architecture checks guard against reintroducing endpoint/service/repository boundary leakage.</done>
</task>

</tasks>

<verification>
- [ ] `pytest -q backend/tests/test_urls.py backend/tests/test_scrapers.py backend/tests/test_health.py`
- [ ] `pytest -q backend/tests/architecture/test_layer_boundaries.py`
</verification>

<success_criteria>
- All tasks completed
- API-02 is satisfied with explicit and test-protected layer boundaries
- URL/scraper/health flows preserve existing behavior needed for MIGR-03
- Boundary regressions become detectable via automated tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-contract-and-structure-hardening/02-02-SUMMARY.md`
</output>
