---
phase: 03-v2-only-cutover-and-legacy-retirement
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - Dockerfile
  - docker-compose.yml
  - backend/Dockerfile
  - frontend/Dockerfile
  - .github/workflows/pull_request.yml
  - .github/workflows/release.yml
  - .github/workflows/phase1-safety-gates.yml
  - scripts/ci/assert_no_legacy_paths.sh
  - scripts/ci/run_cutover_required_checks.sh
  - .github/PULL_REQUEST_TEMPLATE.md
autonomous: true
requirements:
  - MIGR-01
  - MIGR-04
must_haves:
  truths:
    - "Root runtime/build ownership is promoted to canonical backend/frontend paths derived from v2."
    - "PR and main/release pipelines run strict required checks against the post-cutover root stack."
    - "Cutover PRs are constrained by automated legacy-reference guards and required Scope/Risks/Verification sections."
  artifacts:
    - path: docker-compose.yml
      provides: "Canonical root deployment wiring for backend/frontend runtime ownership."
    - path: .github/workflows/pull_request.yml
      provides: "Strict PR gate execution for the cutover-required check suite."
    - path: .github/workflows/release.yml
      provides: "Release pipeline aligned to root backend/frontend ownership and canonical artifact naming."
    - path: scripts/ci/assert_no_legacy_paths.sh
      provides: "Automated blocker for forbidden legacy/v2 route references."
    - path: scripts/ci/run_cutover_required_checks.sh
      provides: "Single command to execute required cutover checks in deterministic order."
    - path: .github/PULL_REQUEST_TEMPLATE.md
      provides: "Mandatory Scope/Risks/Verification documentation contract for cutover PRs."
  key_links:
    - from: scripts/ci/assert_no_legacy_paths.sh
      to: .github/workflows/pull_request.yml
      via: "required CI step for legacy-path enforcement"
      pattern: "assert_no_legacy_paths\\.sh"
    - from: scripts/ci/run_cutover_required_checks.sh
      to: .github/workflows/release.yml
      via: "release workflow gate invocation"
      pattern: "run_cutover_required_checks\\.sh"
    - from: docker-compose.yml
      to: backend/Dockerfile
      via: "build context mapping to promoted backend runtime"
      pattern: "backend/Dockerfile|dockerfile"
    - from: .github/PULL_REQUEST_TEMPLATE.md
      to: .github/workflows/pull_request.yml
      via: "review policy aligned with required gate evidence"
      pattern: "Verification|Risks|Scope"
---

<objective>
Consolidate CI/build/release ownership onto the cutover target stack and establish strict gate enforcement before legacy retirement finalization.

Purpose: Satisfy MIGR-01 and MIGR-04 by making the branch execute a deterministic, blocker-based cutover pipeline.
Output: Root-owned runtime/build path wiring, strict CI/release workflows, and PR/evidence enforcement guardrails.
</objective>

<execution_context>
@/Users/pipepito/.codex/get-shit-done/workflows/execute-plan.md
@/Users/pipepito/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-v2-only-cutover-and-legacy-retirement/03-CONTEXT.md
@.planning/phases/03-v2-only-cutover-and-legacy-retirement/03-RESEARCH.md

@docker-compose.yml
@Dockerfile
@docker-compose.yml
@backend/Dockerfile
@frontend/Dockerfile
@.github/workflows/pull_request.yml
@.github/workflows/release.yml
@.github/workflows/phase1-safety-gates.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote v2 runtime/build ownership to canonical root app paths</name>
  <files>docker-compose.yml, Dockerfile, docker-compose.yml, backend/Dockerfile, frontend/Dockerfile</files>
  <action>Perform the promotion-first cutover of runtime/build ownership: root app structure becomes canonical backend/frontend ownership derived from current v2 stack, and root deployment/container wiring points to that promoted structure. Preserve scraper runtime behavior while removing canonical reliance on legacy root runtime files.</action>
  <verify>test -d backend && test -d frontend && test -f backend/Dockerfile && test -f frontend/Dockerfile</verify>
  <done>Root runtime/build paths are canonicalized to backend/frontend ownership and ready for strict CI gating.</done>
</task>

<task type="auto">
  <name>Task 2: Convert PR/release workflows to strict root-stack gates and canonical artifact policy</name>
  <files>.github/workflows/pull_request.yml, .github/workflows/release.yml, .github/workflows/phase1-safety-gates.yml</files>
  <action>Update workflows to run full required cutover checks on PR and main/release pushes, fail on any required check failure, and align artifact naming/references to canonical root backend/frontend ownership. Remove legacy runtime workflow anchors and enforce deterministic required gate ordering.</action>
  <verify>! rg -n \"wsgi.py|run_dev.py|manage.py|v2/\" .github/workflows/pull_request.yml .github/workflows/release.yml .github/workflows/phase1-safety-gates.yml</verify>
  <done>CI/release pipeline behavior matches strict cutover policy with root-only runtime/build ownership references.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated legacy-reference guardrails and mandatory cutover PR evidence template</name>
  <files>scripts/ci/assert_no_legacy_paths.sh, scripts/ci/run_cutover_required_checks.sh, .github/PULL_REQUEST_TEMPLATE.md</files>
  <action>Create guard scripts that fail on forbidden legacy/v2 path reintroduction and provide a single deterministic gate entrypoint for cutover checks. Add PR template sections requiring Scope, Risks, and Verification evidence to align with locked review/evidence requirements.</action>
  <verify>bash scripts/ci/assert_no_legacy_paths.sh --strict && rg -n \"^## Scope|^## Risks|^## Verification\" .github/PULL_REQUEST_TEMPLATE.md</verify>
  <done>Cutover PRs cannot bypass legacy-reference guardrails and must include required evidence sections.</done>
</task>

</tasks>

<verification>
- [ ] `bash scripts/ci/run_cutover_required_checks.sh --profile quick`
- [ ] `bash scripts/ci/assert_no_legacy_paths.sh --strict`
- [ ] `! rg -n "v2/" .github/workflows/*.yml`
</verification>

<success_criteria>
- Runtime/build ownership is promoted to canonical root backend/frontend paths
- CI/release gates are strict and aligned to root-only cutover ownership
- Legacy-reference guardrails and PR evidence structure are enforced automatically
- MIGR-01 and MIGR-04 have executable, testable implementation coverage
</success_criteria>

<output>
After completion, create `.planning/phases/03-v2-only-cutover-and-legacy-retirement/03-01-SUMMARY.md`
</output>
