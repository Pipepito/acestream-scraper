---
phase: 04-frontend-ux-modernization
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/NavBar.tsx
  - frontend/src/theme.ts
  - frontend/src/components/layout/AppShell.tsx
  - frontend/src/components/layout/PageHeader.tsx
  - frontend/src/components/layout/ContentSection.tsx
  - frontend/src/components/layout/navItems.ts
  - frontend/src/styles/layout.ts
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
must_haves:
  truths:
    - "Operators can navigate major workflows from a clearer shell with consistent page hierarchy."
    - "Primary actions and status context are visible without page-specific layout guesswork."
    - "Shell and navigation behavior remain usable on both desktop and constrained displays."
  artifacts:
    - path: frontend/src/components/layout/AppShell.tsx
      provides: "Reusable shell layout contract for top bar, nav rail/drawer, content framing, and responsive spacing."
    - path: frontend/src/components/layout/navItems.ts
      provides: "Single source of truth for navigation labels, routes, and section grouping."
    - path: frontend/src/components/layout/PageHeader.tsx
      provides: "Shared page title/action/header primitive for operational pages."
    - path: frontend/src/theme.ts
      provides: "Modernized design tokens (spacing, typography, semantic state colors, component density defaults)."
  key_links:
    - from: frontend/src/components/layout/navItems.ts
      to: frontend/src/components/NavBar.tsx
      via: "navigation rendering map"
      pattern: "navItems|map\\("
    - from: frontend/src/components/layout/AppShell.tsx
      to: frontend/src/App.tsx
      via: "route container composition"
      pattern: "AppShell|Routes"
    - from: frontend/src/theme.ts
      to: frontend/src/components/layout/PageHeader.tsx
      via: "typography and spacing token usage"
      pattern: "theme|sx"
---

<objective>
Build a modern, reusable frontend shell and layout foundation before page-level workflow rewrites.

Purpose: Deliver the structural UX baseline required by UI-01/UI-02/UI-03.
Output: Shared shell/navigation/page primitives with a strengthened theme contract.
</objective>

<execution_context>
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-frontend-ux-modernization/04-RESEARCH.md

@frontend/src/App.tsx
@frontend/src/components/NavBar.tsx
@frontend/src/theme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared shell/layout primitives and centralize navigation metadata</name>
  <files>frontend/src/components/layout/AppShell.tsx, frontend/src/components/layout/PageHeader.tsx, frontend/src/components/layout/ContentSection.tsx, frontend/src/components/layout/navItems.ts, frontend/src/styles/layout.ts</files>
  <action>Introduce reusable layout primitives that encode common structure for page header, main content sections, and responsive shell framing. Move route label/icon definitions into a central nav metadata module so page titles, breadcrumbs, and drawer entries share one source of truth.</action>
  <verify>cd frontend && npm run build</verify>
  <done>Layout primitives compile and navigation metadata is centralized with no duplicated route labels in page files.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor app shell and navigation for clearer hierarchy and responsive behavior</name>
  <files>frontend/src/App.tsx, frontend/src/components/NavBar.tsx, frontend/src/components/layout/AppShell.tsx, frontend/src/components/layout/navItems.ts</files>
  <action>Refactor root app composition to use the new shell contract, including consistent toolbar spacing, active-route labeling, grouped navigation, and mobile drawer behavior. Remove debug-only logging and ensure the shell supports constrained widths without content overlap.</action>
  <verify>cd frontend && npm test -- --watch=false --runInBand Dashboard.test.tsx</verify>
  <done>App routes render inside the shared shell and navigation behavior is consistent on desktop/mobile with no layout overlap regressions.</done>
</task>

<task type="auto">
  <name>Task 3: Apply upgraded theme/token baseline to shell and shared primitives</name>
  <files>frontend/src/theme.ts, frontend/src/components/layout/PageHeader.tsx, frontend/src/components/layout/ContentSection.tsx, frontend/src/components/NavBar.tsx</files>
  <action>Upgrade theme tokens to provide stronger information hierarchy and consistent interaction density (typography scale, spacing ramps, semantic state colors, elevation and border standards). Apply tokens in shared shell primitives so downstream pages inherit improved visual structure by default.</action>
  <verify>cd frontend && npm run build</verify>
  <done>Theme and shared primitives expose a coherent visual baseline that downstream pages can reuse without per-page styling drift.</done>
</task>

</tasks>

<verification>
- [ ] `cd frontend && npm run build`
- [ ] `cd frontend && npm test -- --watch=false --runInBand Dashboard.test.tsx`
- [ ] `! rg -n "console\\.log\\(" frontend/src/App.tsx frontend/src/components/NavBar.tsx`
</verification>

<success_criteria>
- Shared shell/navigation primitives are implemented and adopted at app root
- Visual hierarchy and spacing conventions are centralized in theme/layout tokens
- Foundation supports responsive rendering and sets baseline for workflow-page modernization
- UI-01, UI-02, and UI-03 have executable foundation coverage
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-ux-modernization/04-01-SUMMARY.md`
</output>
