---
phase: 05-multi-arch-build-and-runtime-validation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/release.yml
  - .github/workflows/pull_request.yml
  - .github/workflows/multiarch-validation.yml
  - scripts/ci/build_multiarch_images.sh
  - scripts/ci/verify_multiarch_manifest.sh
  - scripts/ci/run_cutover_required_checks.sh
autonomous: true
requirements:
  - COMP-01
must_haves:
  truths:
    - "CI/release pipeline can build container artifacts for linux/arm/v7 and linux/arm64 deterministically."
    - "Multi-arch build execution path is centralized in scripts to prevent workflow drift."
    - "Published/built manifest metadata is validated so missing architecture variants fail the pipeline."
  artifacts:
    - path: scripts/ci/build_multiarch_images.sh
      provides: "Canonical buildx invocation path with explicit platform matrix and tag strategy."
    - path: scripts/ci/verify_multiarch_manifest.sh
      provides: "Manifest-level assertions that required platform variants are present."
    - path: .github/workflows/multiarch-validation.yml
      provides: "Dedicated CI workflow that runs multi-arch build validation on PR/push triggers."
    - path: .github/workflows/release.yml
      provides: "Release-time multi-arch build/publish gate integration."
  key_links:
    - from: scripts/ci/build_multiarch_images.sh
      to: .github/workflows/release.yml
      via: "workflow run step delegates buildx execution to script"
      pattern: "build_multiarch_images\\.sh"
    - from: scripts/ci/verify_multiarch_manifest.sh
      to: .github/workflows/multiarch-validation.yml
      via: "manifest verification step after build"
      pattern: "verify_multiarch_manifest\\.sh"
    - from: .github/workflows/pull_request.yml
      to: scripts/ci/run_cutover_required_checks.sh
      via: "required checks remain chained with new multi-arch validation flow"
      pattern: "run_cutover_required_checks\\.sh"
---

<objective>
Establish deterministic multi-arch build and manifest verification for required ARM targets.

Purpose: Satisfy COMP-01 by hardening CI/release build behavior for `linux/arm/v7` and `linux/arm64`.
Output: Scripted multi-arch build path, manifest verification script, and CI workflow wiring.
</objective>

<execution_context>
@/Users/pipepito/.codex/get-shit-done/workflows/execute-plan.md
@/Users/pipepito/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-multi-arch-build-and-runtime-validation/05-RESEARCH.md

@Dockerfile
@backend/Dockerfile
@frontend/Dockerfile
@.github/workflows/release.yml
@.github/workflows/pull_request.yml
@scripts/ci/run_cutover_required_checks.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement canonical multi-arch build script with explicit platform matrix</name>
  <files>scripts/ci/build_multiarch_images.sh</files>
  <action>Create a reusable build script wrapping Docker Buildx with explicit platform configuration (`linux/amd64,linux/arm/v7,linux/arm64`), deterministic tagging rules, and dry-run/CI modes. Script should support both validation-only and release publishing paths without duplicating build logic in workflow YAML.</action>
  <verify>bash scripts/ci/build_multiarch_images.sh --dry-run --platforms linux/arm/v7,linux/arm64</verify>
  <done>Multi-arch build command is centralized and consistently configurable for PR/release contexts.</done>
</task>

<task type="auto">
  <name>Task 2: Wire release and validation workflows for QEMU + Buildx multi-arch execution</name>
  <files>.github/workflows/release.yml, .github/workflows/pull_request.yml, .github/workflows/multiarch-validation.yml</files>
  <action>Update workflow wiring to set up QEMU and Buildx before multi-arch build steps, invoke the canonical build script, and enforce required gating behavior on pull requests and release paths. Keep existing required checks intact while adding architecture buildability validation.</action>
  <verify>rg -n "setup-qemu|setup-buildx|build_multiarch_images\\.sh|linux/arm/v7|linux/arm64" .github/workflows/release.yml .github/workflows/pull_request.yml .github/workflows/multiarch-validation.yml</verify>
  <done>CI workflows explicitly validate/build required ARM architectures with deterministic setup and invocation paths.</done>
</task>

<task type="auto">
  <name>Task 3: Add manifest verification script and integrate into CI failure gates</name>
  <files>scripts/ci/verify_multiarch_manifest.sh, .github/workflows/multiarch-validation.yml, scripts/ci/run_cutover_required_checks.sh</files>
  <action>Implement manifest-inspection checks that fail when required architectures are absent from built/published image references. Integrate verification into CI gate sequence so architecture omission cannot pass required checks.</action>
  <verify>bash scripts/ci/verify_multiarch_manifest.sh --help</verify>
  <done>Manifest verification exists and is wired into required gates with actionable failure output.</done>
</task>

</tasks>

<verification>
- [ ] `bash scripts/ci/build_multiarch_images.sh --dry-run --platforms linux/arm/v7,linux/arm64`
- [ ] `rg -n "setup-qemu|setup-buildx|linux/arm/v7|linux/arm64" .github/workflows/release.yml .github/workflows/multiarch-validation.yml`
- [ ] `bash scripts/ci/verify_multiarch_manifest.sh --help`
</verification>

<success_criteria>
- Multi-arch build path is deterministic and script-driven
- Required ARM variants are part of enforced CI/release build matrix
- Manifest validation blocks missing architecture variants
- COMP-01 has executable implementation coverage
</success_criteria>

<output>
After completion, create `.planning/phases/05-multi-arch-build-and-runtime-validation/05-01-SUMMARY.md`
</output>

