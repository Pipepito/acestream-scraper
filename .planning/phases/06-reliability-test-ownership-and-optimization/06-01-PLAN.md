---
phase: 06-reliability-test-ownership-and-optimization
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/run_v2_test_suite.sh
  - .github/workflows/pull_request.yml
  - scripts/ci/run_cutover_required_checks.sh
  - backend/tests/contracts/test_urls_contracts.py
  - backend/tests/regression/test_legacy_behavior_parity.py
  - frontend/src/__tests__/AcestreamChannelsPage.test.tsx
  - docs/testing/test-ownership-matrix.md
  - tests/README.md
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
must_haves:
  truths:
    - "Critical regression coverage is owned by canonical v2 test locations (`backend/tests`, `frontend/src/__tests__`)."
    - "CI and local required-check flows use one canonical v2 test runner instead of implicit/distributed commands."
    - "Legacy root `tests/` is no longer an authoritative execution target after replacement coverage lands."
  artifacts:
    - path: scripts/ci/run_v2_test_suite.sh
      provides: "Single canonical regression entrypoint for backend + frontend required checks."
    - path: docs/testing/test-ownership-matrix.md
      provides: "Explicit mapping from legacy tests to canonical v2 replacements and ownership status."
    - path: backend/tests/regression/test_legacy_behavior_parity.py
      provides: "Coverage migration for high-value legacy behavior assertions into canonical backend tests."
    - path: frontend/src/__tests__/AcestreamChannelsPage.test.tsx
      provides: "Expanded UI regression coverage for core operational channel-management workflows."
  key_links:
    - from: scripts/ci/run_v2_test_suite.sh
      to: .github/workflows/pull_request.yml
      via: "PR workflow invokes canonical v2 regression command"
      pattern: "run_v2_test_suite\\.sh"
    - from: scripts/ci/run_v2_test_suite.sh
      to: scripts/ci/run_cutover_required_checks.sh
      via: "cutover checks delegate test authority to canonical v2 runner"
      pattern: "run_v2_test_suite\\.sh"
    - from: docs/testing/test-ownership-matrix.md
      to: tests/README.md
      via: "legacy suite retirement/quarantine documented with replacement references"
      pattern: "legacy|replacement|canonical"
---

<objective>
Rebuild and enforce canonical regression-test ownership under the root v2 stack, then retire redundant legacy test authority.

Purpose: Satisfy QUAL-01 and QUAL-02 by making test ownership explicit, executable, and CI-enforced.
Output: Canonical v2 test runner, ownership matrix, replacement tests, and legacy-suite decommission guidance.
</objective>

<execution_context>
@/Users/pipepito/.codex/get-shit-done/workflows/execute-plan.md
@/Users/pipepito/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-reliability-test-ownership-and-optimization/06-RESEARCH.md

@.github/workflows/pull_request.yml
@scripts/ci/run_cutover_required_checks.sh
@backend/tests/
@frontend/src/__tests__/
@tests/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create canonical v2 regression runner and wire it into required checks</name>
  <files>scripts/ci/run_v2_test_suite.sh, .github/workflows/pull_request.yml, scripts/ci/run_cutover_required_checks.sh</files>
  <action>Implement a deterministic regression runner that executes required backend and frontend test subsets for quick/full profiles and integrate it into PR + cutover check flows so ownership is command-driven and not implicit.</action>
  <verify>bash scripts/ci/run_v2_test_suite.sh --profile quick</verify>
  <done>Required-check flows execute canonical v2 test ownership through one script entrypoint.</done>
</task>

<task type="auto">
  <name>Task 2: Port high-value legacy assertions into canonical backend/frontend test locations</name>
  <files>backend/tests/contracts/test_urls_contracts.py, backend/tests/regression/test_legacy_behavior_parity.py, frontend/src/__tests__/AcestreamChannelsPage.test.tsx</files>
  <action>Migrate still-relevant assertions from root `tests/` into `backend/tests` and `frontend/src/__tests__` so behavior coverage is preserved without legacy harness dependencies.</action>
  <verify>PYTHONPATH=backend backend/venv/bin/pytest -q backend/tests/contracts backend/tests/regression && (cd frontend && npm test -- --watch=false --runInBand AcestreamChannelsPage.test.tsx)</verify>
  <done>Legacy behavior coverage exists in canonical paths with passing tests.</done>
</task>

<task type="auto">
  <name>Task 3: Decommission root legacy test authority with explicit replacement documentation</name>
  <files>docs/testing/test-ownership-matrix.md, tests/README.md</files>
  <action>Document old-to-new test ownership mapping, mark root `tests/` as legacy reference only (or remove executable authority entirely if replacements are complete), and ensure future contributors use canonical v2 suites.</action>
  <verify>test ! -d tests || (test -f tests/README.md && rg -n \"legacy|reference|do not use for required checks\" tests/README.md)</verify>
  <done>Legacy test suite is no longer treated as canonical and replacement ownership is explicit.</done>
</task>

</tasks>

<verification>
- [ ] `bash scripts/ci/run_v2_test_suite.sh --profile quick`
- [ ] `PYTHONPATH=backend backend/venv/bin/pytest -q backend/tests`
- [ ] `cd frontend && npm test -- --watch=false --runInBand Dashboard.test.tsx`
- [ ] `test ! -d tests || test -f tests/README.md`
</verification>

<success_criteria>
- Canonical v2 test ownership is explicit and enforced by CI-required commands
- Root legacy tests are replaced/decommissioned as authoritative execution targets
- Critical backend/frontend regressions are covered in canonical locations
- QUAL-01 and QUAL-02 have executable implementation coverage
</success_criteria>

<output>
After completion, create `.planning/phases/06-reliability-test-ownership-and-optimization/06-01-SUMMARY.md`
</output>
