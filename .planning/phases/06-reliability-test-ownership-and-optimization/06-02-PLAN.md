---
phase: 06-reliability-test-ownership-and-optimization
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - backend/main.py
  - backend/app/api/endpoints/background_tasks.py
  - backend/app/services/background_task_status_service.py
  - backend/app/services/task_service.py
  - backend/app/tasks/url_scraping_task.py
  - backend/app/tasks/epg_refresh_task.py
  - backend/app/tasks/channel_status_task.py
  - backend/app/api/endpoints/warp.py
  - backend/tests/test_background_tasks.py
  - backend/tests/test_task_service.py
  - backend/tests/test_warp.py
  - docs/ops/reliability-runbook.md
autonomous: true
requirements:
  - QUAL-03
  - QUAL-01
must_haves:
  truths:
    - "Background-task status behavior is backed by real scheduler/job state instead of placeholder in-memory defaults."
    - "Scheduler lifecycle and task execution paths are idempotent, observable, and regression-tested under failure scenarios."
    - "High-impact operational endpoints return deterministic, actionable errors under integration failure conditions."
  artifacts:
    - path: backend/app/services/background_task_status_service.py
      provides: "Scheduler-backed status service with stable task-state representation."
    - path: backend/main.py
      provides: "Single route ownership for background-task status and hardened startup/shutdown task lifecycle."
    - path: backend/tests/test_background_tasks.py
      provides: "Regression assertions for background-task endpoint behavior and failure contracts."
    - path: backend/tests/test_task_service.py
      provides: "Reliability tests for scheduler idempotency and task lifecycle edge cases."
    - path: docs/ops/reliability-runbook.md
      provides: "Operational guidance for diagnosing and recovering task/scheduler/runtime reliability issues."
  key_links:
    - from: backend/app/services/background_task_status_service.py
      to: backend/app/api/endpoints/background_tasks.py
      via: "endpoint delegates status payload generation to scheduler-backed service"
      pattern: "get_all_statuses|task_name|next_run"
    - from: backend/app/services/task_service.py
      to: backend/main.py
      via: "startup/shutdown orchestration and scheduled-job registration"
      pattern: "start\\(|shutdown\\(|add_interval_task"
    - from: backend/tests/test_task_service.py
      to: backend/app/services/task_service.py
      via: "idempotency and failure-path reliability assertions"
      pattern: "start|shutdown|remove_task"
---

<objective>
Burn down high-impact runtime reliability defects in scheduler, task status, and operational error paths.

Purpose: Satisfy QUAL-03 while extending canonical test ownership for reliability-critical flows.
Output: Hardened scheduler/task lifecycle, unambiguous status endpoint behavior, and regression tests/runbook for operational failures.
</objective>

<execution_context>
@/Users/pipepito/.codex/get-shit-done/workflows/execute-plan.md
@/Users/pipepito/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-reliability-test-ownership-and-optimization/06-RESEARCH.md
@.planning/phases/06-reliability-test-ownership-and-optimization/06-01-PLAN.md

@backend/main.py
@backend/app/api/endpoints/background_tasks.py
@backend/app/services/background_task_status_service.py
@backend/app/services/task_service.py
@backend/app/tasks/url_scraping_task.py
@backend/app/tasks/epg_refresh_task.py
@backend/app/tasks/channel_status_task.py
@backend/app/api/endpoints/warp.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove background-task status route ambiguity and back status data with scheduler state</name>
  <files>backend/main.py, backend/app/api/endpoints/background_tasks.py, backend/app/services/background_task_status_service.py</files>
  <action>Consolidate `/api/v1/background-tasks/status` to one route ownership path, eliminate duplicate route definitions, and make status payloads originate from scheduler/job metadata rather than placeholder in-memory defaults.</action>
  <verify>! rg -n \"@status_router.get\\(\\\"/api/v1/background-tasks/status\\\"\\)\" backend/main.py && PYTHONPATH=backend backend/venv/bin/pytest -q backend/tests/test_background_tasks.py</verify>
  <done>Background-task status endpoint behavior is deterministic and backed by runtime scheduler state.</done>
</task>

<task type="auto">
  <name>Task 2: Harden scheduler/task lifecycle and error handling in periodic task execution paths</name>
  <files>backend/app/services/task_service.py, backend/app/tasks/url_scraping_task.py, backend/app/tasks/epg_refresh_task.py, backend/app/tasks/channel_status_task.py</files>
  <action>Implement idempotent scheduler startup/shutdown guards, improve task exception context/logging, and ensure periodic task execution failures are surfaced without leaving scheduler state inconsistent.</action>
  <verify>PYTHONPATH=backend backend/venv/bin/pytest -q backend/tests/test_task_service.py backend/tests/test_background_tasks.py</verify>
  <done>Task scheduler lifecycle and periodic task reliability are verified with deterministic regression tests.</done>
</task>

<task type="auto">
  <name>Task 3: Fix high-impact endpoint failure semantics and publish reliability runbook</name>
  <files>backend/app/api/endpoints/warp.py, backend/tests/test_warp.py, backend/tests/test_error_contracts.py, docs/ops/reliability-runbook.md</files>
  <action>Normalize high-impact integration failure paths (notably WARP/task operations) to stable error codes and actionable contexts, then document troubleshooting/recovery procedures for operators.</action>
  <verify>PYTHONPATH=backend backend/venv/bin/pytest -q backend/tests/test_warp.py backend/tests/test_error_contracts.py && rg -n \"scheduler|background task|recovery|diagnostic\" docs/ops/reliability-runbook.md</verify>
  <done>Operational error behavior is consistent and accompanied by actionable runbook guidance.</done>
</task>

</tasks>

<verification>
- [ ] `PYTHONPATH=backend backend/venv/bin/pytest -q backend/tests/test_background_tasks.py backend/tests/test_task_service.py`
- [ ] `PYTHONPATH=backend backend/venv/bin/pytest -q backend/tests/test_warp.py backend/tests/test_error_contracts.py`
- [ ] `! rg -n "@status_router.get\\(\"/api/v1/background-tasks/status\"\\)" backend/main.py`
- [ ] `rg -n "scheduler|background task|recovery" docs/ops/reliability-runbook.md`
</verification>

<success_criteria>
- Background-task status endpoint has single ownership and scheduler-backed data
- Scheduler/task lifecycle is idempotent and failure-resilient
- High-impact integration failures emit stable, actionable error contracts
- QUAL-03 has executable reliability-hardening coverage
</success_criteria>

<output>
After completion, create `.planning/phases/06-reliability-test-ownership-and-optimization/06-02-SUMMARY.md`
</output>
