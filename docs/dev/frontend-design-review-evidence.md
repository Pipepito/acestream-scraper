# Frontend Design Review Evidence

## Shared Layout Primitives
- Light theme: method=`CI=true npm test -- --runInBand --watch=false src/__tests__/layoutPrimitives.test.tsx src/__tests__/Dashboard.test.tsx` covering `createAppTheme('light')` for shared primitives and dashboard; result=pass; notes=verified the page header, `Controls`, `Recent Activity`, and `Background Tasks` render with semantic typography and surface tokens in light mode.
- Dark theme: method=`CI=true npm test -- --runInBand --watch=false src/__tests__/layoutPrimitives.test.tsx src/__tests__/Dashboard.test.tsx` covering `createAppTheme('dark')` for shared primitives and dashboard; result=pass; notes=verified the same shared layout artifacts render under dark tokens without light-only color assumptions.
- Reduced motion: method=prefers-reduced-motion verification via `frontend/src/theme.ts` motion-token review (`durationReduced` and `durationNone`) plus shared primitive inspection for `PageHeader`, `ContentSection`, `NavBar`, and `AppShell`; result=no touched layout primitive depends on animation timing for usability; notes=motion stays centralized in the theme contract and these primitives add no custom transition that would block reduced-motion users.
- Mobile width: method=reviewed `xs`/`sm` breakpoint behavior in `PageHeader.tsx`, `ContentSection.tsx`, `NavBar.tsx`, and `AppShell.tsx` alongside the dashboard regression render; result=header actions stack, content sections stay full width, and the shell keeps the temporary drawer path for smaller screens; notes=responsive behavior remains API-stable and token-driven.
- Desktop width: method=reviewed `sm`/`md` breakpoint behavior in the same shared primitives and confirmed the dashboard regression still renders the desktop shell structure; result=header actions align inline, the permanent drawer/app bar shell remains intact, and dashboard sections preserve desktop grouping; notes=main shell background now comes from semantic surface tokens instead of implicit defaults.
- Keyboard path: method=reviewed the dashboard shell path from nav link -> page header actions -> dashboard controls for semantic button/link structure and absence of custom `tabIndex` or focus traps in touched primitives; result=keyboard reachability remains in DOM order with standard MUI focus handling preserved; notes=selected nav styling moved to semantic tokens without replacing native focus behavior.
- Loading / empty / error / success states: method=ran `src/__tests__/Dashboard.test.tsx` for dashboard loading, error, and success-feedback cases and reviewed dashboard empty-state copy in `Recent Activity` and `Background Tasks`; result=pass; notes=verified artifact=`dashboard`, including progress indicator, error alert with retry, success/error snackbar feedback, and empty-list fallback text.

## Supporting Pages And Shared Action Surfaces
- Light theme: method=reviewed touched supporting pages (`Settings.tsx`, `WARP.tsx`, `Health.tsx`, `Playlist.tsx`, `Search.tsx`, `TVChannelDetail.tsx`, `EPGChannelDetail.tsx`, `NotFound.tsx`) against current light semantic tokens and ran `CI=true npm test -- --watch=false`; result=pass; notes=full suite now passes after aligning `EPGBulkMatch.test.tsx` with the section-based EPG layout and keeping supporting-page regressions green.
- Dark theme: method=reviewed the same touched pages against `createAppTheme('dark')` tokens in `theme.ts` plus shared section/header token usage; result=pass by inspection; notes=pages consume semantic surface/text/status tokens and do not introduce light-only assumptions in the touched code paths.
- Reduced motion: method=reviewed `theme.ts` motion contract (`durationReduced`, `durationNone`) and touched pages/components for custom transitions; result=pass by inspection; notes=no page-local timing values or essential animation-dependent interactions were introduced in the touched supporting-page flows.
- Mobile width: method=reviewed `xs`/`sm` layout behavior in shared `PageHeader`/`ContentSection` plus supporting-page action areas such as WARP header actions and Settings forms; result=pass by inspection; notes=actions wrap or stack at small widths without hiding primary controls.
- Desktop width: method=reviewed `md` layout behavior on dense shared action areas and two-column supporting-page sections; result=pass by inspection; notes=desktop layouts preserve grouped operational sections without one-off wrappers.
- Zoom / reflow: method=reviewed dense shared action areas (`PageHeader` actions and `ContentSection` actions) and supporting forms for wrap behavior under narrower space; result=pass by inspection; notes=the touched shared wrappers use flex-wrap and avoid fixed-width action rails that would force horizontal overflow sooner.
- Keyboard path: method=ran supporting-page interaction tests (`src/__tests__/SupportingPages.test.tsx`) and reviewed the primary flow for touched areas: Settings toggle/save paths, WARP connect/disconnect + mode select, and shared dialogs/action groups; result=pass for touched flows; notes=standard MUI controls remain tabbable in DOM order with no custom focus trap introduced by the normalization changes.
- Visible focus states: method=reviewed theme focus-visible contract and touched interactive controls using MUI button/input/select primitives; result=pass by inspection; notes=the work keeps framework focus behavior intact and does not remove or override visible focus styling on touched controls.
- Contrast and non-color status communication: method=reviewed touched status surfaces in Settings and WARP plus semantic status tokens in `theme.ts`; result=pass by inspection; notes=status surfaces pair severity text labels like `Online`, `Needs attention`, `Connected`, and `Disconnected` with semantic Alert/Chip treatment so meaning does not rely on color alone.
- Screen-reader-friendly announcements / semantics: method=reviewed touched loading and feedback surfaces plus test assertions around alerts/headings; result=pass; notes=loading and outcome states use semantic `Alert`, progress indicators, headings, and button labels, while Settings/WARP success-error feedback remains text-explicit.
- Warm accent usage: method=reviewed touched files for warning/warm-accent usage against the semantic theme contract; result=pass by inspection; notes=warm accents remain sparse and purposeful, limited to semantic warning/status surfaces rather than decorative one-off highlights.
- Loading / empty / warning / error / success states: method=ran `src/__tests__/SupportingPages.test.tsx` and reviewed touched supporting pages; result=pass; notes=confirmed AppID load/update failure handling in Settings and mode/status sync in WARP, with existing empty/warning/error/success messaging preserved where those states exist.

## Final Frontend Verification
- Lint: method=`CI=true npx eslint "src/**/*.{ts,tsx}"`; result=pass; notes=frontend lint completed with no remaining warnings after cleanup in `AcestreamChannels.tsx`, `EPGChannelDetail.tsx`, and `TVChannels.tsx`.
- Full tests: method=`CI=true npm test -- --watch=false`; result=pass; notes=17 suites passed, 72 tests passed, 0 failed; remaining output is limited to known React Testing Library `act(...)` and React Router future-flag warnings in the test environment.
- Build: method=`npm run build`; result=pass; notes=production build compiled successfully after verification cleanup and updated bundle output was produced under `frontend/build/`.
- TypeScript: method=`npx tsc --noEmit`; result=pass; notes=TypeScript verification completed cleanly after restoring the `GridSortModel` type-only import for `AcestreamChannels.tsx`.

## Frontend Hardening Foundation
- Light theme: method=`CI=true npm test -- --runInBand --watch=false src/__tests__/apiErrors.test.ts src/__tests__/errorUtils.test.ts src/__tests__/formatters.test.ts src/__tests__/sharedStateComponents.test.tsx src/__tests__/layoutPrimitives.test.tsx src/__tests__/ChannelTable.test.tsx src/__tests__/AcestreamChannelsPage.test.tsx` with `createAppTheme('light')` coverage in `sharedStateComponents.test.tsx`, `layoutPrimitives.test.tsx`, and `AcestreamChannelsPage.test.tsx`; result=pass; notes=verified light semantic tokens across shared state surfaces, layout primitives, and the Acestream Channels shell without regressions.
- Dark theme: method=`CI=true npm test -- --runInBand --watch=false src/__tests__/sharedStateComponents.test.tsx src/__tests__/layoutPrimitives.test.tsx`; result=pass; notes=verified the same hardening surfaces render in `createAppTheme('dark')` with semantic background, text, border, and selected-state tokens instead of light-only assumptions.
- Mobile width: method=`CI=true npm test -- --runInBand --watch=false src/__tests__/ChannelTable.test.tsx src/__tests__/layoutPrimitives.test.tsx` plus review of `ChannelTable.tsx` `useMediaQuery(theme.breakpoints.down('md'))` action-column sizing and wrap behavior; result=pass; notes=confirmed compact action surfaces stay reachable, wrap safely, and preserve empty/no-results overlays at smaller widths.
- Desktop width: method=`CI=true npm test -- --runInBand --watch=false src/__tests__/ChannelTable.test.tsx src/__tests__/layoutPrimitives.test.tsx`; result=pass; notes=confirmed dense desktop layouts keep formatted timestamps, grouped channel actions, semantic app-shell surfaces, and stable section structure at full-width render sizes.
- Keyboard path: method=`CI=true npm test -- --runInBand --watch=false src/__tests__/AcestreamChannelsPage.test.tsx src/__tests__/ChannelTable.test.tsx` with button/role assertions for `Try again`, `Check All Statuses`, row action groups, and copy/edit/delete/toggle controls; result=pass; notes=verified keyboard-addressable controls keep explicit accessible names and retry/action flows remain operable without custom focus traps.
- Loading / empty / error / retry / success states: method=`CI=true npm test -- --runInBand --watch=false src/__tests__/AcestreamChannelsPage.test.tsx src/__tests__/sharedStateComponents.test.tsx src/__tests__/ChannelTable.test.tsx`; result=pass; notes=covered retryable offline channel errors, preserved filters during partial-load failures, empty-state regions, inline warning/error/success live-region semantics, clipboard/save failure messaging, and successful status-check snackbar behavior constraints.
- Long-text / i18n stress: method=`CI=true npm test -- --runInBand --watch=false src/__tests__/layoutPrimitives.test.tsx src/__tests__/sharedStateComponents.test.tsx src/__tests__/ChannelTable.test.tsx`; result=pass; notes=verified emoji, RTL, CJK, and long translated-looking titles, descriptions, action labels, and multilingual channel/group strings wrap or truncate safely with `overflowWrap: break-word`, preserved landmarks, and accessible action labels.
- TypeScript hardening verification: method=`npx tsc --noEmit`; result=pass; notes=closed branch-specific test typing gaps by giving mocked Axios configs and DataGrid test doubles explicit types so hardening coverage now satisfies strict compile checks as well as runtime tests.
- Full regression verification: method=`CI=true npm test -- --watch=false` and `npm run build`; result=pass; notes=the full frontend suite and production build both succeeded after the minimal test-typing fixes, with only pre-existing test-environment warnings remaining in Jest output.

## Responsive Shell And Inventory Adaptation
- Light theme: method=`CI=true npm test -- --runInBand --watch=false src/__tests__/layoutPrimitives.test.tsx src/__tests__/NavBarResponsive.test.tsx src/__tests__/TVChannelsTable.test.tsx src/__tests__/TVChannelsPageResponsive.test.tsx src/__tests__/Dashboard.test.tsx` with `createAppTheme('light')`; result=pass; notes=verified the responsive shell, TV channel inventory, and shared page composition all render against the light semantic tokens without introducing light-only assumptions.
- Dark theme: method=`CI=true npm test -- --runInBand --watch=false src/__tests__/layoutPrimitives.test.tsx`; result=pass; notes=verified the shared responsive shell tokens and responsive page/header-section primitives continue to render under `createAppTheme('dark')` after the shell and layout changes.
- Phone width (~375px): method=`CI=true npm test -- --runInBand --watch=false src/__tests__/NavBarResponsive.test.tsx src/__tests__/TVChannelsTable.test.tsx src/__tests__/TVChannelsPageResponsive.test.tsx` with deterministic `useMediaQuery` phone mocks; result=pass; notes=confirmed phone mode keeps the drawer trigger visible, switches TV channels to stacked summaries with touch-friendly explicit actions, collapses filters behind `Show Filters`, and opens create/edit dialogs full-screen.
- Desktop width (~1024px): method=`CI=true npm test -- --runInBand --watch=false src/__tests__/NavBarResponsive.test.tsx src/__tests__/TVChannelsTable.test.tsx src/__tests__/Dashboard.test.tsx`; result=pass; notes=confirmed the persistent desktop drawer remains in place, the dense TV channel data grid stays active above the phone breakpoint, and dashboard/shared page sections preserve their desktop alignment.
- Wide desktop width (1440px+): method=`CI=true npm test -- --runInBand --watch=false src/__tests__/layoutPrimitives.test.tsx src/__tests__/NavBarResponsive.test.tsx src/__tests__/TVChannelsPageResponsive.test.tsx`; result=pass; notes=verified the wide shell content limit expands at the shared threshold and `TVChannels` can place the inventory region beside the supporting filter rail instead of stacking everything in a single lane.
- Keyboard path: method=`CI=true npm test -- --runInBand --watch=false src/__tests__/NavBarResponsive.test.tsx src/__tests__/TVChannelsTable.test.tsx`; result=pass; notes=verified keyboard flow from persistent desktop nav links into the first visible TV channel row action, with DOM order and accessible button labels preserved.
- Mobile management path: method=`CI=true npm test -- --runInBand --watch=false src/__tests__/TVChannelsTable.test.tsx src/__tests__/TVChannelsPageResponsive.test.tsx`; result=pass; notes=covered the balanced phone path of keeping `Refresh`/`Add TV Channel` visible first, revealing filters on demand, and entering create/edit management dialogs without relying on desktop-sized overlays.
- Warning cleanup verification: method=`CI=true npm test -- --watch=false` after moving shared test helpers out of Jest test-match paths, normalizing router tests onto shared future flags, and updating warning-prone interaction tests to explicit `react` `act(...)` coverage plus current Testing Library packages; result=pass; notes=full frontend suite completed at `23` suites / `141` tests with the earlier responsive-helper false failure removed and the targeted router/interaction warning sources cleaned up in the touched responsive and supporting tests.

## TV Channels Design System Normalization
- Light theme: method=`CI=true npm test -- --runInBand --watch=false src/__tests__/layoutPrimitives.test.tsx src/__tests__/theme.test.tsx src/__tests__/TVChannelsTable.test.tsx src/__tests__/TVChannelsPageResponsive.test.tsx`; result=pass; notes=verified the normalized shell contract, filter surface, TV channel inventory, and page dialogs against `createAppTheme('light')` semantic tokens.
- Dark theme: method=`CI=true npm test -- --runInBand --watch=false src/__tests__/layoutPrimitives.test.tsx src/__tests__/theme.test.tsx`; result=pass; notes=verified the updated shell token names, wide split-layout helpers, and shared layout primitives under `createAppTheme('dark')` without reintroducing light-only assumptions.
- Phone width (~375px): method=`CI=true npm test -- --runInBand --watch=false src/__tests__/TVChannelsPageResponsive.test.tsx src/__tests__/TVChannelsTable.test.tsx` with deterministic phone-mode `useMediaQuery` mocks; result=pass; notes=confirmed primary actions remain visible, filters collapse behind an explicit affordance, phone filter controls use larger touch-safe sizing, the inventory renders stacked summaries with supporting metadata, and create/edit dialogs stay full-screen.
- Desktop width (~1024px): method=`CI=true npm test -- --runInBand --watch=false src/__tests__/TVChannelsTable.test.tsx src/__tests__/TVChannelsPageResponsive.test.tsx`; result=pass; notes=confirmed the desktop inventory stays in a semantic region-wrapped data grid, desktop loading/empty/page-empty states are text-explicit, and the TV Channels page keeps the inventory as the primary working area.
- Wide desktop width (1440px+): method=`CI=true npm test -- --runInBand --watch=false src/__tests__/layoutPrimitives.test.tsx src/__tests__/TVChannelsPageResponsive.test.tsx`; result=pass; notes=verified `TVChannels` now consumes the shared `getWidePageSplitLayout(...)` contract instead of page-local grid math and places filters in the supporting region.
- Keyboard path: method=`CI=true npm test -- --runInBand --watch=false src/__tests__/TVChannelsTable.test.tsx`; result=pass; notes=verified the first desktop row action remains keyboard-focusable with its explicit accessible label inside the normalized inventory region.
- Guided CRUD path: method=`CI=true npm test -- --runInBand --watch=false src/__tests__/TVChannelsPageResponsive.test.tsx`; result=pass; notes=verified create/edit dialogs expose grouped sections (`Channel basics`, `Optional metadata`, `Visibility & favorites`), delete uses a product-consistent confirmation dialog, cancel keeps deletion untouched, and explicit confirmation is required before the destructive mutation runs.
- TypeScript verification: method=`npx tsc --noEmit`; result=pass; notes=the widened shared layout contract, `visibleFields` filter configuration, and `TVChannels` dialog/filter changes compile cleanly.
- Full regression verification: method=`CI=true npm test -- --watch=false`; result=pass; notes=full frontend suite completed at `23` suites / `151` tests after the normalization pass with no remaining `ReactDOMTestUtils.act` deprecation warnings once the installed `@testing-library/react` version was refreshed to the declared dependency.
- Production build: method=`npm run build`; result=pass; notes=optimized production bundle compiled successfully after the TV Channels normalization changes.

## Test Harness Dependency Cleanup
- Root cause: method=`npm ls @testing-library/react react react-dom` plus inspection of `frontend/node_modules/@testing-library/react/dist/act-compat.js`; result=identified; notes=the worktree had an invalid installed `@testing-library/react@13.4.0` even though `frontend/package.json` declared `^14.3.1`, and the older installed copy was the one surfacing the deprecated `react-dom/test-utils` warning path in Jest.
- Dependency refresh: method=`npm install`; result=pass; notes=the frontend install state was refreshed and `npm ls @testing-library/react` now resolves to `14.3.1`, updating `frontend/package-lock.json` to match the declared dependency.
- Warning regression check: method=`CI=true npm test -- --runInBand --watch=false src/__tests__/layoutPrimitives.test.tsx src/__tests__/TVChannelsTable.test.tsx src/__tests__/TVChannelsPageResponsive.test.tsx`; result=pass; notes=the previously warning-heavy targeted suites now complete with no `ReactDOMTestUtils.act` deprecation output.

## CRA To Vite Security Migration
- Audit remediation strategy: method=`npm audit --json` plus dependency-tree inspection; result=completed via toolchain migration; notes=`react-scripts@5.0.1` was the root of the unresolved `webpack-dev-server`, `workbox-*`, `@svgr/webpack`, `css-minimizer-webpack-plugin`, and related advisories, so the frontend was migrated to Vite plus standalone Jest instead of using the unsafe `npm audit fix --force` path.
- Runtime compatibility: method=`npm test -- --runInBand src/__tests__/runtimeConfig.test.ts src/__tests__/index.test.tsx`; result=pass; notes=verified the new runtime config helpers preserve local-backend API behavior, playlist download URL behavior, test-only grid virtualization disabling, and the app bootstrap entrypoint.
- Full regression verification: method=`npm test -- --runInBand`; result=pass; notes=the migrated frontend test harness completed at `24` suites / `156` tests with the Jest-based suite preserved after removing CRA.
- TypeScript verification: method=`npx tsc --noEmit`; result=pass; notes=the Vite runtime types, standalone Jest config, and runtime abstraction compile cleanly.
- Production build: method=`npm run build`; result=pass; notes=Vite emitted the production bundle successfully under `frontend/dist/`.
- Backend artifact handoff: method=`npm run build:backend`; result=pass; notes=the Vite production bundle copied successfully into `backend/frontend_build/` through the updated copy script and build contract.
- Audit status: method=`npm audit --json`; result=pass; notes=the frontend dependency graph now reports `0` vulnerabilities after removing CRA.
- Bundle split regression coverage: method=`npm test -- --runInBand src/__tests__/viteConfig.test.ts`; result=pass; notes=added a regression guard that the Vite config preserves explicit manual chunks for React, MUI, data, icons, and the TV Channels page.
- Bundle size follow-up: method=`npm run build`; result=improved-with-warning; notes=manual chunking split the former monolithic app bundle into `react-vendor`, `mui-vendor`, `mui-icons`, `data-vendor`, and `tv-channels-page`, reducing the main entry chunk substantially; the remaining warning is now isolated to `@mui/x-data-grid` (`mui-data-grid`) rather than the general application bundle.

## Frontend Hardening Sweep
- TV Channels load and retry resilience: method=`npm test -- --runInBand src/__tests__/TVChannelsPageResponsive.test.tsx`; result=pass; notes=verified catalog load failures now show explicit recovery copy and a retry action instead of a dead-end generic alert.
- TV Channels form validation and mutation resilience: method=`npm test -- --runInBand src/__tests__/TVChannelsPageResponsive.test.tsx`; result=pass; notes=confirmed whitespace-only channel names are blocked client-side, update failures keep dialogs open, preserved user input remains visible, and retryable API messages are surfaced inline.
- Filter boundary and empty-result recovery: method=`npm test -- --runInBand src/__tests__/TVChannelsPageResponsive.test.tsx`; result=pass; notes=verified trimmed filter input, page reset/clamping on narrower results, and explicit zero-match recovery guidance with reset support.
- Long-text / multilingual table resilience: method=`npm test -- --runInBand src/__tests__/TVChannelsTable.test.tsx`; result=pass; notes=confirmed long multilingual names, RTL text, emoji, and long metadata remain readable in compact TV channel cards without losing action controls.
- Shared empty/status resilience: method=`npm test -- --runInBand src/__tests__/sharedStateComponents.test.tsx`; result=pass; notes=verified long multilingual titles, descriptions, and multiple actions wrap safely in `EmptyState` and `InlineStatusNotice` without dropping semantics or forcing action overflow.

## TV Channels Distillation
- Page hierarchy simplification: method=`npm test -- --runInBand src/__tests__/TVChannelsPageResponsive.test.tsx`; result=pass; notes=verified the page header copy is shorter, the inventory section no longer repeats nearby guidance, and the filters region remains subordinate in the supporting column.
- Filter utility simplification: method=`npm test -- --runInBand src/__tests__/TVChannelsPageResponsive.test.tsx`; result=pass; notes=confirmed the filter surface uses shorter utility-focused copy while preserving required fields, mobile disclosure, apply/reset behavior, and hardened empty-result recovery.
- Dialog simplification: method=`npm test -- --runInBand src/__tests__/TVChannelsPageResponsive.test.tsx`; result=pass; notes=verified create/edit dialogs keep validation and failure recovery while replacing heavier titled subsections with a flatter field flow and one lightweight optional-details label.
- State copy distillation: method=`npm test -- --runInBand src/__tests__/TVChannelsTable.test.tsx`; result=pass; notes=loading and page-empty inventory states now use shorter copy while keeping one obvious next step for the user.

## Shared Primitives Distillation
- Layout primitive rhythm: method=`npm test -- --runInBand src/__tests__/layoutPrimitives.test.tsx`; result=pass; notes=`PageHeader` and `ContentSection` now use tighter subtitle/description rhythm while preserving semantics, wrapping, responsive action behavior, and wide-layout hooks.
- Dashboard consumer validation: method=`npm test -- --runInBand src/__tests__/Dashboard.test.tsx`; result=pass; notes=the calmer shared defaults still support a dense operational page with intact page-header hierarchy, actions, and section scanability.
- TV Channels consumer validation: method=`npm test -- --runInBand src/__tests__/TVChannelsPageResponsive.test.tsx`; result=pass; notes=the simplified shared defaults carry through the already-distilled TV Channels flow without requiring page-local layout fixes.

## Dashboard Distillation
- Header hierarchy simplification: method=`npm test -- --runInBand src/__tests__/Dashboard.test.tsx`; result=pass; notes=verified the header subtitle is shorter and the CTA hierarchy is clearer, with `EPG` as the primary next-step action and the remaining routes still accessible as secondary links.
- Readiness summary simplification: method=`npm test -- --runInBand src/__tests__/Dashboard.test.tsx`; result=pass; notes=confirmed the narrative three-step operational block was replaced by a tighter `System readiness` summary while preserving stream, WARP, and scheduler visibility plus warning-first treatment.
- Utility and metadata simplification: method=`npm test -- --runInBand src/__tests__/Dashboard.test.tsx`; result=pass; notes=the former controls area now reads as a lighter `Dashboard tools` section, and recent activity/background task rows use more compact metadata formatting without removing required timing, status, or user/error information.

## Shared Foundation And Accessibility Backbone
- Light theme: method=`npm test -- --runInBand src/__tests__/index.test.tsx src/__tests__/layoutPrimitives.test.tsx src/__tests__/SupportingPages.test.tsx`; result=pass; notes=verified persisted light-mode bootstrap behavior, shell theme controls, the Settings appearance section, and shared status surfaces under the light token set.
- Dark theme: method=`npm test -- --runInBand src/__tests__/index.test.tsx src/__tests__/layoutPrimitives.test.tsx`; result=pass; notes=verified the shared theme controller, shell toggle, and shared layout primitives render correctly when the saved or system-resolved mode is dark.
- Phone width (~375px): method=`npm test -- --runInBand src/__tests__/NavBarResponsive.test.tsx src/__tests__/BatchAssignDialog.test.tsx src/__tests__/BulkOperations.test.tsx`; result=pass; notes=confirmed the shell keeps the mobile drawer trigger visible, the top-bar theme toggle remains reachable, and the touched dialogs now switch via responsive hooks instead of raw viewport checks.
- Desktop width (~1024px): method=`npm test -- --runInBand src/__tests__/NavBarResponsive.test.tsx src/__tests__/EPG.test.tsx`; result=pass; notes=verified the desktop shell no longer duplicates page titles, the utility-strip theme toggle precedes nav links in DOM order, and the section-based EPG page remains stable without reintroducing a tab-led hierarchy.
- Keyboard path: method=`npm test -- --runInBand src/__tests__/NavBarResponsive.test.tsx src/__tests__/EPGChannelDetail.test.tsx src/__tests__/TVChannelDetail.test.tsx`; result=pass; notes=covered keyboard reachability from the shell toggle into grouped nav links plus explicit radio/checkbox selection controls in the touched EPG and TV detail association flows.
- Loading / error / success states: method=`npm test -- --runInBand src/__tests__/SupportingPages.test.tsx src/__tests__/BatchAssignDialog.test.tsx src/__tests__/BulkOperations.test.tsx src/__tests__/EPGChannelDetail.test.tsx src/__tests__/TVChannelDetail.test.tsx`; result=pass; notes=verified page and dialog loading states now include contextual text, mapping/association dialogs expose explicit empty and error handling, and success feedback persists until the user dismisses the touched dialogs.
- Explicit action labels: method=`npm test -- --runInBand src/__tests__/Scraper.test.tsx src/__tests__/EPG.test.tsx src/__tests__/TVChannelDetail.test.tsx`; result=pass; notes=confirmed touched icon-only controls expose action-specific accessible names in Scraper, EPG inventory/source rows, and TV detail Acestream rows.

## Frontend Bold Redesign
- Light theme: method=`npm test -- --runInBand src/__tests__/theme.test.tsx src/__tests__/layoutPrimitives.test.tsx src/__tests__/Dashboard.test.tsx src/__tests__/HealthBoldLayout.test.tsx src/__tests__/SearchBoldLayout.test.tsx src/__tests__/PlaylistBoldLayout.test.tsx`; result=pass; notes=verified the measured-bold shell tokens, scraper-led dashboard hero, readiness-led Health summary, guided Search hero, and playlist primary-path treatment under `createAppTheme('light')`.
- Dark theme: method=`npm test -- --runInBand src/__tests__/theme.test.tsx src/__tests__/layoutPrimitives.test.tsx`; result=pass; notes=verified the expanded `shell` and `hero` token groups preserve the same semantic shape and shared shell hierarchy in `createAppTheme('dark')` without drifting into generic dark-admin styling.
- Phone width (~375px): method=`npm test -- --runInBand src/__tests__/NavBarResponsive.test.tsx src/__tests__/PlaylistBoldLayout.test.tsx`; result=pass; notes=confirmed the drawer trigger remains visible, shell background treatment remains intact, playlist primary actions stay reachable, and advanced controls remain optional on smaller widths.
- Desktop width (~1280px): method=`npm test -- --runInBand src/__tests__/layoutPrimitives.test.tsx src/__tests__/Dashboard.test.tsx`; result=pass; notes=verified the shell content stage expands correctly, navigation stays persistent, and the dashboard shows one dominant scraper/readiness focal area instead of flat equal-weight metrics.
- Keyboard path: method=`npm test -- --runInBand src/__tests__/NavBarResponsive.test.tsx src/__tests__/Dashboard.test.tsx src/__tests__/Search.test.tsx`; result=pass; notes=verified visible focus remains on shell controls, dashboard primary actions stay in the main action group, and Search keeps accessible checkbox/button controls for the primary add flow.
- Loading state: method=`npm test -- --runInBand src/__tests__/Dashboard.test.tsx src/__tests__/SupportingPages.test.tsx`; result=pass; notes=dashboard and settings loading states still pair progress indicators with readable status text after the redesign changes.
- Empty state: method=`npm test -- --runInBand src/__tests__/SearchBoldLayout.test.tsx src/__tests__/Dashboard.test.tsx`; result=pass by test plus code-path review; notes=Search now includes recovery guidance for zero results and Dashboard keeps explicit fallback copy when activity/task lists are empty.
- Warning / error state: method=`npm test -- --runInBand src/__tests__/Dashboard.test.tsx src/__tests__/HealthBoldLayout.test.tsx`; result=pass; notes=Dashboard and Health retain plain-language warning/error treatment, with hero/status areas using labels like `Attention needed` and clear next-step text rather than color alone.
- Success-feedback state: method=`npm test -- --runInBand src/__tests__/Dashboard.test.tsx src/__tests__/SupportingPages.test.tsx`; result=pass; notes=existing snackbar and alert feedback remains readable after the visual refresh and still communicates outcomes with explicit text.

## Frontend Bold Redesign Phase 2
- Light theme: method=`npm test -- --runInBand src/__tests__/EPG.test.tsx src/__tests__/WarpBoldLayout.test.tsx src/__tests__/SettingsBoldLayout.test.tsx src/__tests__/SupportingPages.test.tsx`; result=pass; notes=verified the measured-bold EPG source-readiness hero, the WARP tunnel-readiness summary, and the Settings control-center summary under `createAppTheme('light')` while keeping the existing supporting-page flows intact.
- Dark theme: method=theme-token inspection against the touched `EPG.tsx`, `WARP.tsx`, and `Settings.tsx` hero surfaces plus shared `hero` and `surface` semantic tokens; result=pass by inspection; notes=the phase-2 pages continue to consume semantic token groups rather than light-only values, so the new focal summaries preserve the same structure in dark mode.
- Phone width (~375px): method=responsive layout review of the touched hero/action surfaces in `EPG.tsx`, `WARP.tsx`, and `Settings.tsx`; result=pass by inspection; notes=hero side panels drop to full width, header actions stack where needed, and primary controls remain visible without introducing hidden mobile-only dependencies.
- Desktop width (~1280px): method=`npm test -- --runInBand src/__tests__/EPG.test.tsx src/__tests__/WarpBoldLayout.test.tsx src/__tests__/SettingsBoldLayout.test.tsx`; result=pass; notes=verified the redesigned pages open with one dominant operational summary while preserving dense secondary sections for source operations, tunnel controls, and settings forms.
- Keyboard path: method=`npm test -- --runInBand src/__tests__/EPG.test.tsx src/__tests__/SupportingPages.test.tsx`; result=pass; notes=confirmed explicit button and checkbox labels remain intact for key EPG row actions, WARP connect/disconnect and mode controls, and Settings refresh/toggle/save paths without custom focus handling.
- Loading state: method=`npm test -- --runInBand src/__tests__/SupportingPages.test.tsx`; result=pass; notes=Settings keeps its contextual loading copy and the touched pages preserve progress indicators for inventory, tunnel actions, and source refresh work.
- Warning / error state: method=`npm test -- --runInBand src/__tests__/SupportingPages.test.tsx`; result=pass; notes=WARP and Settings preserve explicit alert text for disconnection, engine issues, and configuration failures, while EPG keeps plain-language warning/error snackbar handling for source and channel actions.
- Success-feedback state: method=`npm test -- --runInBand src/__tests__/SupportingPages.test.tsx`; result=pass; notes=the phase-2 redesign leaves the existing success snackbars and positive status messaging readable and action-linked instead of relying on visual treatment alone.

## Frontend Bold Redesign Pipeline Cluster
- Light theme: method=`npm test -- --runInBand src/__tests__/Scraper.test.tsx src/__tests__/AcestreamChannelsPage.test.tsx src/__tests__/TVChannelsPageResponsive.test.tsx`; result=pass; notes=verified the shared `Sources` -> `Extracted channels` -> `TV organization` stage summary plus the intake, routing, and downstream organization hero treatments under `createAppTheme('light')`.
- Dark theme: method=theme-token inspection across `Scraper.tsx`, `AcestreamChannels.tsx`, and `TVChannels.tsx`; result=pass by inspection; notes=the pipeline summaries and hero blocks continue to use semantic `hero`, `surface`, and shell accent tokens rather than light-only color values, preserving the same hierarchy in dark mode.
- Scraper phone width (~375px): method=responsive review of `Scraper.tsx` hero/action wrapping and stage tiles; result=pass by inspection; notes=the intake summary stacks safely, primary actions remain visible, and `Configured URLs` stays directly below the hero without hiding scrape controls.
- Scraper desktop width (~1280px): method=`npm test -- --runInBand src/__tests__/Scraper.test.tsx`; result=pass; notes=verified the intake summary renders above `Configured URLs`, keeps `Sources` as the current stage, and points users toward extracted-channel review without overpowering the working table.
- AcestreamChannels phone width (~375px): method=responsive review of `AcestreamChannels.tsx` header actions, hero side panels, and inventory-first section order; result=pass by inspection; notes=actions stack cleanly, the routing summary drops to full width, and the inventory remains primary while filters stay secondary.
- AcestreamChannels desktop width (~1280px): method=`npm test -- --runInBand src/__tests__/AcestreamChannelsPage.test.tsx`; result=pass; notes=verified the strongest cluster page opens with routing guidance, keeps `Channels` before `Filters`, and preserves assignment/bulk-action surfaces under the new hierarchy.
- TVChannels phone width (~375px): method=`npm test -- --runInBand src/__tests__/TVChannelsPageResponsive.test.tsx`; result=pass; notes=confirmed the inventory remains primary, the filter toggle stays attached to the inventory section, zero-result recovery guidance remains visible, and the shared pipeline summary survives the smaller layout.
- TVChannels desktop width (~1280px): method=`npm test -- --runInBand src/__tests__/TVChannelsPageResponsive.test.tsx`; result=pass; notes=verified the downstream organization summary renders above the inventory, `TV Channel Inventory` stays ahead of `Filters`, and filter apply/reset actions reset pagination to the first page.
- Keyboard path: method=`npm test -- --runInBand src/__tests__/Scraper.test.tsx src/__tests__/AcestreamChannelsPage.test.tsx src/__tests__/TVChannelsPageResponsive.test.tsx src/__tests__/TVChannelsTable.test.tsx`; result=pass; notes=verified explicit labels and reachable controls for scrape row actions, extracted-channel assignment/status actions, and TV-channel organization/edit/delete flows without custom focus handling.
- Loading state: method=`npm test -- --runInBand src/__tests__/AcestreamChannelsPage.test.tsx src/__tests__/TVChannelsTable.test.tsx`; result=pass; notes=Acestream inventory remains usable while group suggestions load, and TV channel inventory loading guidance remains explicit after the redesign.
- Empty state: method=`npm test -- --runInBand src/__tests__/Scraper.test.tsx src/__tests__/TVChannelsPageResponsive.test.tsx`; result=pass; notes=Scraper now distinguishes between no sources configured and sources disabled, while TVChannels preserves zero-result recovery guidance with an obvious reset path.
- Warning / error state: method=`npm test -- --runInBand src/__tests__/AcestreamChannelsPage.test.tsx src/__tests__/TVChannelsPageResponsive.test.tsx`; result=pass; notes=the cluster retains clear retryable load failures, deletion/update error feedback, and page-level warnings without relying on visual treatment alone.
- Success-feedback state: method=`npm test -- --runInBand src/__tests__/AcestreamChannelsPage.test.tsx src/__tests__/TVChannelsPageResponsive.test.tsx`; result=pass; notes=check-all status success/error snackbars, TV channel create/update/delete notices, and scraper completion feedback remain text-explicit under the new hierarchy.
- Cross-page hierarchy: method=review of `Scraper.tsx`, `AcestreamChannels.tsx`, and `TVChannels.tsx` plus targeted test output; result=pass by inspection with test support; notes=`Scraper` stays the lightest intake summary, `AcestreamChannels` is the strongest routing surface in the cluster, and `TVChannels` reads as the calmer downstream organization stage while all three share one consistent pipeline language.
