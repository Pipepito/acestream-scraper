# v2.1.0 upgrade notes

These notes describe the changes prepared for v2.1.0.

## ARM64 playback and WARP reliability

The bundled ARM engine now uses its distribution's correct startup entry point,
fixing `mod_detected` playback failures that could occur despite healthy version
and status responses. The pinned 3.2.17-fix engine and persistent identity are
retained; no downgrade is required.

Playback and checking engines coordinate ownership of their shared DNS listener,
and the surviving engine takes over when its owner stops. Their Python resolver
also follows container DNS changes when WARP connects or disconnects, retaining
the last valid configuration during an incomplete rewrite.

With `ENABLE_WARP=true` and `WARP_ENABLE_NAT=true`, startup now waits for WARP to
report Connected before launching engines. A responsive CLI alone is insufficient.
WARP remains opt-in and requires the documented container capabilities and TUN
device. See [Docker setup](../../wiki/Docker.md).

Fresh ARM64 installations on Apple Silicon resolved and played all three tested
IDs with WARP connected; the dedicated checking engine verified media for all
three. Tuner and Acexy streaming, DNS reconnects, a full container restart and
1080p browser playback were also verified. The same IDs failed on the tested
direct route, so `failed to load content` can require a network-routing change
rather than a longer timeout. This evidence does not establish playback on every
ARM board; ARMv7 remains experimental. See the
[controlled comparison](../ops/arm64-mod-detected.md).

## Documentation publishing

The manual release job updates the wiki after successful, non-dry-run latest
promotion. Develop validates documentation without publishing it. GitHub Pages
continues serving `main/docs`; Docker Hub overview text in `docs/dockerhub/` is
maintained for manual copying to Docker Hub.

## Legacy environment names continue working

v2.1 retains the six legacy names below. Startup logs and Overview → Storage show
name-only deprecation warnings and their replacements. A nonempty canonical value
wins when both names are set; otherwise the legacy value remains effective.
This also applies to `.env` files. Keep values and persistent volume paths unchanged
when renaming. `ENABLE_LEGACY_ENV_ALIASES` is no longer required and does not disable
compatibility. Retirement is deferred beyond v2.1.

| Deprecated name | Preferred name |
|---|---|
| `SCRAPER_DB_URL` | `DATABASE_URL` |
| `LEGACY_DB_URL` | `LEGACY_DATABASE_URL` |
| `ZERONET_BASE_URL` | `ZERONET_URL` |
| `CORS_ALLOW_ORIGINS` | `CORS_ORIGINS` |
| `FRONTEND_STATIC_DIR` | `FRONTEND_BUILD_PATH` |
| `ACESTREAM_ENGINE_URL` | `ACE_ENGINE_URL` |

Normal startup creates a database backup before applying pending migrations.
Upgrade/restore coverage verifies the previous schema, settings, sources and
channels remain available in that backup.

## Extraction recipes

Sources can use HTML selectors, record-scoped regex, or JSON field paths. The
installed builder can fetch, preview and save source rules. The public tools hub
can author recipes from pasted samples without connecting to an installation.
See the [extraction builder guide](../../wiki/Extraction-Recipes.md) for examples,
limitations, troubleshooting and safe import/export.

## Source fetch protection

HTTP, IPFS gateway, ZeroNet page/iframe, nested M3U, custom recipe and EPG fetches
now connect only to DNS answers validated by the outbound guard. Redirects are
validated at every hop, including redirects to literal IP addresses. HTTPS keeps
the source hostname for certificate validation, SNI and Host headers.

`ALLOW_PRIVATE_SCRAPE_TARGETS=true` remains the default for existing LAN setups.
Set it to `false` for an exposed installation; configured ZeroNet/IPFS gateway
hosts remain exempt. IPv4/IPv6 cloud metadata addresses are always blocked.
Source fetches do not inherit `HTTP_PROXY`/`HTTPS_PROXY` environment proxies, which
could otherwise resolve destinations outside the guard. This does not change
container network routing such as WARP. Remote-player/media-server LAN clients
retain their separate destination policy.

## Advanced stream statistics (#122)

Manual, bulk and scheduled stream checks retain the latest valid peer count and
P2P download/upload rates with their observation time. Acestream Channels shows
these alongside independently measured media bitrate on desktop and phone views.
Skipped checks preserve observations; missing values remain unknown. These short
samples do not imply verified signal or sustained throughput.
See [stream statistics](../../wiki/Stream-Statistics.md) for collection steps,
units and limitations. History, automatic playlist thresholds and loop detection
remain outside this version.

## Container storage

Overview lists discovered mounted directories and configured data directories,
allocated file size, filesystem capacity and available space. Scans are bounded,
cached and independent of the app database. See [storage information](../../wiki/Storage.md).

## Pre-merge hardening

Preview uploads are authenticated and admitted before JSON parsing, with two slots,
a bounded temporary spool, a 30-second upload deadline and a 64 MiB + 128 KiB JSON
envelope limit. Decoded source samples still support 32 MiB. Visual HTML preparation
and field pickers have a separate 2 MiB rendering budget; HTML additionally permits
at most 20,000 markup delimiters. Heavy samples retain raw selection and installed
extraction tests. The synchronous source client retries validated DNS addresses
within one connection budget; real HTTPS tests cover SNI, certificate trust and
hostname rejection.
